package com.math.clockangle.model.calc;

import com.math.clockangle.model.clock.Clock;

import com.math.clockangle.model.clock.Angle;

public interface AngleCalc {
	
	public Angle calculateAngle(Clock clock);
	
}
